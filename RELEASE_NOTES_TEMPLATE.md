# CORE Cowork Max Plugin — v{{VERSION}} Release Notes

**Date:** {{DATE}}

**SHA256:**
```
{{SHA256}}  core-claude-cowork-max-plugin-v{{VERSION}}.zip
```

**Verify your download:**
```bash
shasum -a 256 -c core-claude-cowork-max-plugin-v{{VERSION}}.SHA256SUMS
```

---

## Install

| Channel | Instructions |
|---|---|
| **Marketplace** | Search "CORE max" in Cowork → Settings → Plugins |
| **SharePoint** | [Link to org SharePoint page — update for each release] |
| **GitHub** | Download from [Releases](https://github.com/dbatesai/core-claude-cowork-max-plugin/releases/tag/v{{VERSION}}) |

**First-time install:** open any Cowork session after installing. Your DM will introduce themselves and walk you through setup (~3 minutes). One Cowork restart (Cmd+Q + reopen) activates the live dashboard.

**Upgrading:** replace the plugin in Cowork Settings → Plugins. Your project data (`~/.core/`) and DM identity (`~/.core/dm-profile.md`) are preserved across upgrades.

---

## What's in this release

[Fill in changes for this release. The CHANGELOG.md has the full history.]

### New in v{{VERSION}}

- [Change 1]
- [Change 2]

### Fixed in v{{VERSION}}

- [Fix 1]

### Known issues

- [Any known issue with a workaround, if applicable]

---

## Supply chain

This release was built from `dbatesai/core-claude-cowork-max-plugin` tag `v{{VERSION}}`. All per-asset CI gates passed (MCP server build + pytest, HTML validation, SVG lint, template schema, connector docs, plugin manifest, bundled skill checksums). The SHA256 above covers the complete plugin zip.

PGP signature (optional): `core-claude-cowork-max-plugin-v{{VERSION}}.zip.asc` — verify with `gpg --verify`.

Signing key: `https://github.com/dbatesai.gpg`

---

*Generated by `scripts/release-coordinator.sh {{VERSION}}` on {{DATE}}.*
